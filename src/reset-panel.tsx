import {useState} from 'react';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
import {AlertDialog,AlertDialogContent,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel} from '@/components/ui/alert-dialog';
export default function ResetPanel({act,busy}:{act:any,busy:boolean}){const [open,setOpen]=useState(false),[word,setWord]=useState('');return <><div className="archive"><h3>Alle App-Daten zurücksetzen</h3><p>Löscht Getränke, Striche, Umlagen, Belege, Bestände, Bestellungen, Einkaufslisten, Veranstaltungen, Bewerbungen, Notizen und Korrekturanfragen in dieser App. Mitgliedskonten und Rollen bleiben erhalten.</p><p>Erstelle vor einem Reset eine Datenbanksicherung.</p><Button variant="outline" onClick={()=>{setWord('');setOpen(true)}}>Alles zurücksetzen …</Button></div><AlertDialog open={open} onOpenChange={setOpen}><AlertDialogContent><AlertDialogTitle>Alle App-Daten endgültig löschen?</AlertDialogTitle><AlertDialogDescription>Dieser Reset löscht auch die archivierten Belege und Bestandsaufnahmen. Die Aktion lässt sich in der App nicht rückgängig machen. Die angelegten Mitgliedskonten bleiben erhalten.</AlertDialogDescription><label>Zur Bestätigung exakt „reset“ eingeben<Input autoComplete="off" autoCapitalize="none" spellCheck={false} value={word} onChange={e=>setWord(e.target.value)}/></label><AlertDialogFooter><AlertDialogCancel disabled={busy}>Abbrechen</AlertDialogCancel><Button variant="destructive" disabled={busy||word!=='reset'} onClick={async()=>{if(await act('resetAll',{confirmation:word})){setOpen(false);setWord('')}}}>Jetzt endgültig zurücksetzen</Button></AlertDialogFooter></AlertDialogContent></AlertDialog></>}
