import {Button} from '@/components/ui/button';
const labels:any={guest:'Gäste',member:'Mitglieder',crew:'Crew',admin:'Admin',master:'Master'};
export function PreviewChoices({data,onSelect}:{data:any,onSelect:(role:string)=>void}){if(!data?.canPreview)return null;return <section className="panel"><h2>Ansicht wechseln</h2><p>Prüfe, welche Bereiche die jeweilige Rolle sieht. Deine echte Rolle bleibt unverändert. In der Vorschau kannst du keine Daten ändern.</p><div className="management-grid">{['guest','member','crew','admin',...(data.viewerRole==='master'?['master']:[])].map(role=><button className="management-tile" key={role} onClick={()=>onSelect(role)}><strong>{labels[role]}</strong><span>Ansicht schreibgeschützt öffnen →</span></button>)}</div><p className="muted">Persönliche Listen und Dienste zeigen gegebenenfalls deine eigenen Daten, nicht die eines anderen Mitglieds.</p></section>}
export function PreviewBanner({role,onExit}:{role:string,onExit:()=>void}){return <aside className="preview-banner" role="status"><div><strong>VORSCHAU: {labels[role]}</strong><span>Nur ansehen · Deine Kontorolle bleibt unverändert</span></div><Button onClick={onExit}>Vorschau beenden</Button></aside>}
