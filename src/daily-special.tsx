import {useState} from 'react';
import {Martini,Sparkles} from 'lucide-react';
import {Input} from '@/components/ui/input';
import {Button} from '@/components/ui/button';
export const today=()=>new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Berlin'}).format(new Date());
export function SpecialCard({special}:{special:any}){if(!special?.active||special.day!==today())return null;return <aside className="daily-special"><div className="special-art" aria-hidden="true"><span className="special-orbit"/><Sparkles className="special-spark"/><Martini className="special-glass"/></div><div className="special-copy"><span className="special-label">★ NUR HEUTE · SPECIAL DES TAGES</span><h3>{special.title}</h3><p>{special.description}</p><strong className="special-price">{new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}).format(special.price/100)}</strong><span className="special-note">Frag uns an der Theke.</span></div></aside>}
export default function SpecialEditor({special,act,busy}:{special:any,act:any,busy:boolean}){const [title,setTitle]=useState(special?.title||''),[description,setDescription]=useState(special?.description||''),[price,setPrice]=useState(special?String(special.price/100):''),[day,setDay]=useState(special?.day||today());return <section className="panel special-editor"><h3>Special des Tages</h3><p>Erscheint ausschließlich auf der Gästekarte am gewählten Datum. Die Drinklist bleibt unverändert.</p><form className="event-form" onSubmit={async e=>{e.preventDefault();await act('saveSpecial',{title,description,price:Math.round(Number(price)*100),day,active:true})}}><label>Titel<Input required maxLength={80} value={title} placeholder="Zum Beispiel: Sunset Spritz" onChange={e=>setTitle(e.target.value)}/></label><label>Beschreibung<Input maxLength={300} value={description} placeholder="Fruchtig, frisch und nur heute an der Bar." onChange={e=>setDescription(e.target.value)}/></label><label>Gästepreis in €<Input required type="number" min="0" max="1000" step="0.01" inputMode="decimal" value={price} onChange={e=>setPrice(e.target.value)}/></label><label>Gültig am<Input required type="date" value={day} onChange={e=>setDay(e.target.value)}/></label><div className="actions"><Button disabled={busy}>Special speichern & aktivieren</Button>{special?.active&&<Button type="button" variant="outline" disabled={busy} onClick={()=>act('disableSpecial')}>Special ausblenden</Button>}</div></form><p role="status">{special?.active?`Gespeichert für ${special.day}: ${special.title}`:'Derzeit kein Special aktiviert.'}</p></section>}
